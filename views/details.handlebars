<style>
  @import
  url("https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700;800&display=swap");
  :root { --purple: #3629cb; --red: #ac0000; --white: #ffffff; --gray: #dfdfdf;
  --black: #000000; --green: #018948; } * { margin: 0; padding: 0; box-sizing:
  border-box; list-style: none; text-decoration: none; font-family: "Poppins",
  sans-serif; } .navbar { width: 100%; height: 3.6rem; display: flex;
  align-items: center; justify-content: flex-end; padding: 0rem 1rem;
  background-color: var(--purple); } .navbar nav ul { display: flex;
  align-items: center; gap: 1rem; } .navbar nav ul li a { font-family:
  "Poppins"; color: var(--white); } .navbar nav ul li a:hover { color: #e0e0e0;
  } /* ####################### */ .body { flex: 1; display: flex;
  justify-content: center; flex-wrap: wrap; width: 100%; height: 100%; padding:
  1rem; } .card { display: flex; flex-direction: column; justify-content:
  space-between; width: 20rem; height: 16.2rem; margin: 10px; padding: 1rem;
  background-color: var(--purple); border: 2px solid var(--purple);
  border-radius: 12px; } .card-header { display: flex; align-items: center;
  justify-content: space-between; } .card-title { font-size: 18pt; font-weight:
  bold; text-overflow: ellipsis; white-space: nowrap; color: var(--white); }
  .card-category { padding: 0.6rem; background-color: #2c20ad; border-radius:
  6px; } .card-category span { color: var(--white); font-weight: bold; }
  .card-price { color: var(--white); } .card-description { display: block;
  height: 4.7em; text-overflow: ellipsis; overflow: hidden; font-size: 13pt;
  color: var(--gray); } .card-footer { display: flex; gap: 0.3rem; } .button {
  display: flex; align-items: center; justify-content: center; width: 100%;
  height: 2.3rem; font-weight: bold; background-color: transparent; border:
  none; border-radius: 6px; cursor: pointer; transition: 0.6s ease; }
  .button:hover { opacity: 0.8; } .delete { border: 1px solid var(--red);
  background-color: var(--red); color: var(--white); } .edit { border: 1px solid
  var(--green); background-color: var(--green); color: var(--white); } .view {
  height: 4rem; background-color: #2c20ad; border: 1px solid #2c20ad; color:
  var(--white); margin-top: 1rem; margin-bottom: 0.5rem; } .view:hover {
  background-color: #231991; cursor: pointer; } .content-header { display: flex;
  align-items: center; flex-wrap: wrap; gap: 1rem; } .main-content { margin:
  1rem; } .form { width: 370px; display: flex; align-items: flex-start;
  flex-direction: column; margin-top: 1rem; } .form label { display: flex;
  align-items: flex-start; flex-direction: column; margin-top: 0.6rem; } .form
  label span { font-size: 16pt; font-weight: bold; } .form input { width: 100%;
  padding: 0.6rem; border-radius: 6px; border: 1px solid var(--black); color:
  var(--black); } .form textarea { width: 370px; height: 10rem; resize: none;
  padding: 0.7rem; } .form-multi { display: flex; align-items: center; gap:
  1rem; } .form button { width: 100%; height: 2.6rem; margin-top: 1rem;
  background-color: var(--purple); color: var(--white); border: none;
  border-radius: 6px; cursor: pointer; } .footer { position: fixed; bottom: 0;
  display: flex; align-items: center; justify-content: center; width: 100%;
  height: 2.8rem; color: var(--white); padding: 1rem; background-color:
  var(--purple); } @media (max-width: 645px) { .card { width: 100%; } }

</style>
<main class="main-content">
  <a href="/" style="font-weight: bold; color:#2c20ad;">Back</a>
  {{#each result}}
    <div class="content-header">
      <h1>{{this.title}}</h1>
      <div class="card-category">
        <span>{{this.category}}</span>
      </div>
    </div>
    <span style="font-weight: 600; font-size: 16pt;">R$ {{this.price}}</span>
    <p style="font-weight: 600; color: #696969">{{this.description}}</p>
  {{/each}}
</main>